export interface Project {
  title: string;
  description: string;
  stack: string[];
  image?: string;
  repoUrl?: string;
  liveUrl?: string;
}

export const projects: Project[] = [
  // Agrega tus proyectos aquí cuando estén listos
];
